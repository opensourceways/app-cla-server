appname = app-cla-server
httpport = "${HTTP_PORT||8080}"
runmode = "${RUN_MODE||prod}"
autorender = false
copyrequestbody = true
EnableDocs = false

EnableService = "${Enable_Service||sign}"
app_conf = "${APP_CONFIG_FILE||./conf/app.conf.yaml}"
